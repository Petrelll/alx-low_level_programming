#include <stdio.h>

/**
 * main - Entry point
 * Return: 0 (Successful)
 */

int main(void) {

	char b;
	b = 'a';
	while
	       	(b <= 'z')  {
			putchar(b);
			b++;
		}
			putchar('\n');

    return 0;
}
